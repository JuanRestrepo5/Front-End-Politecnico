import { Routes } from '@angular/router';
import { PerfilUsuarioComponent } from './perfil-usuario.component';

export const perfilUsuarioRoutes: Routes = [
  { path: '', component: PerfilUsuarioComponent }
];